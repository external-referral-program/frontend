export const baseRoute = '/'

export const SECTION_IDS = {
  QUESTIONS: 'questions',
  VACANCIES: 'vacancies',
  RECOMMEND: 'recommend',
}

export const SECTION_PATHS = {
  QUESTIONS: baseRoute + '#' + SECTION_IDS.QUESTIONS,
  VACANCIES: baseRoute + '#' + SECTION_IDS.VACANCIES,
  RECOMMEND: baseRoute + '#' + SECTION_IDS.RECOMMEND,
}
